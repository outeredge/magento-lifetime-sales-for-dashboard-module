language: php
php:
  - 7.0
  - 7.1
directories:
  - $HOME/.composer/cache/files
  - $HOME/.config/composer
install: composer global require "squizlabs/php_codesniffer=*" --no-interaction --prefer-source
script:
  - composer install --no-interaction --prefer-source
  - ~/.config/composer/vendor/bin/phpcs .
